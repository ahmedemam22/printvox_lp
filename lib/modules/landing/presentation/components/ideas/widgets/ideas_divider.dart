import 'package:flutter/material.dart';

class IdeasDivider extends StatelessWidget {
  const IdeasDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 1,
      height: 60,
      color: Colors.white24,
    );
  }
}
